# Python GIF Creator 📸

> All input images **MUST have the exact same dimensions (width × height)** before processing. Passing images with different dimensions will cause `imageio` to crash or output a corrupted GIF.

---

## ⚡ Prerequisites

1. **Python 3.x**
2. **`imageio` library:**
   ```bash
   pip install imageio
